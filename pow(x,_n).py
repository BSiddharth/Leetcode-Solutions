# https://leetcode.com/problems/pow(x,-n)/description/
# git add . && git commit -m "completed pow(x,_n)" && git push && exit

class Solution:
    def myPow(self, x: float, n: int) -> float:
        return x**n
