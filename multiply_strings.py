# https://leetcode.com/problems/multiply-strings/description/
# git add . && git commit -m "completed multiply_strings" && git push && exit

class Solution:
    def multiply(self, num1: str, num2: str) -> str:
        # stupid question
        return str(int(num1)*int(num2))
